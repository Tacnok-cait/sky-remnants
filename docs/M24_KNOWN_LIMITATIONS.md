# M24 RC Known Limitations / Pending Gates

No release-blocking gameplay defect was found by the Linux automated core pass. The following are explicitly pending and must not be called passed until user-local Windows verification supplies results.

1. **Historical world migration** — current workspace lacks the user's retained world folders. Test only copies of retained accepted worlds.
2. **Official Jade/JEI runtime** — current workspace lacks the previously accepted official JARs and cannot reach their download hosts. Core-without-compat and no-stub-leakage are validated.
3. **Dual-client multiplayer** — Linux one-server/two-client orchestration was attempted but did not finish before the outer environment timed out. This is not a PASS. Windows must recheck airship state, scan/navigation isolation and supply-manifest data safety.

Accepted M19 non-blocker remains: a second already-open supply menu does not live-refresh another client's ghost-target edit, but reopening reflects authoritative server state and prior acceptance found no item duplication/loss/stale-close overwrite.
